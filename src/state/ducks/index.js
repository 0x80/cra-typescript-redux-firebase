/**
 * Here we are exporting the duck reducers for easy import in the store. Note
 * that the names used for export here typically respond to the keys that
 * reference the ducks in the app state.
 */
export { default as stub } from './stub';
